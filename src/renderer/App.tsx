import React from 'react';

const App: React.FC = () => {
    return (
        <div className="p-4 text-gray-800">
            <h1 className="text-2xl font-bold mb-4">Hello from Electron + Vite + React + Tailwind CSS!</h1>
        </div>
    );
};

export default App;
